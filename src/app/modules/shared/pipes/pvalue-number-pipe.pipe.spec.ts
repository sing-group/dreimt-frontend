import { PvalueNumberPipePipe } from './pvalue-number-pipe.pipe';

describe('PvalueNumberPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PvalueNumberPipePipe();
    expect(pipe).toBeTruthy();
  });
});
